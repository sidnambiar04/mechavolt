<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= serviceName %> - Shops</title>
    <link rel="stylesheet" href="/css/service-shops.css">
</head>
<body>
    <header>
        <h1>Shops for <%= serviceName %></h1>
        <a href="/admin/dashboard" class="back-btn">Back to Dashboard</a>
    </header>
    <main>
        <section class="shop-list">
            <% if (shops.length > 0) { %>
                <ul>
                    <% shops.forEach(shop => { %>
                        <li>
                            <h3><%= shop.name %></h3>
                            <p><strong>Contact:</strong> <%= shop.contact || 'N/A' %></p>
                            <p><strong>Address:</strong> <%= shop.address || 'N/A' %></p>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No shops listed for this service.</p>
            <% } %>
        </section>
    </main>
</body>
</html>